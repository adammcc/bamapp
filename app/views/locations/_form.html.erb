<%= form_with(model: location, local: true, html: {multipart: true}) do |form| %>
  <% if location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong><%= form.label :name, class: "block-label" %></strong>
    <%= form.text_field :name %>
  </div><br>

  <div class="field">
    <strong><%= form.label :description, class: "block-label" %></strong>
    <%= form.text_area :description %>
  </div><br>

  <div class="field">
    <%= file_field_tag "[attachments][][image]", multiple: true %>
  </div><br>

  <div class="actions ">
    <%= form.submit 'Submit', :class => 'btn btn-primary' %>
  </div><br>
<% end %>
